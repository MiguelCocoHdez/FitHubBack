server:
  port: 8761

eureka:
  instance:
    hostname: localhost  #Este es el host del servidor
  client:
    registerWithEureka: false  #esto es para que no se registre en eureka a si mismo
    fetchRegistry: false
    serviceUrl: 
      defaultZone: http://localhost:${server.port}/eureka/